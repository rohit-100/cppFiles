#include<bits/stdc++.h>
using namespace std;

int main(){

    int t;
    cin>>t;
    while(t--){
        int n,x;cin>>n>>x;
        vector<int>arr(n+1);for(int i=1;i<=n;i++)cin>>arr[i];
        vector<int>hash(n+1);
        for(int i=1;i<=n;i++)hash[arr[i]] = i;

    
    }
}
